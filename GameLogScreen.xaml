<Page
    x:Class="BaseballGameCreator.GameLogScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BaseballGameCreator"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Margin="15,15,15,15">
            <TextBlock Text="Game Log" FontSize="30"/>
            <TextBox Header="Enter any extra notes here:" x:Name="extraNotesX" Margin="0,15,0,0" FontSize="20"/>
            <StackPanel x:Name="mainGameLogSP" Height="600" HorizontalAlignment="Left">
                <TextBox x:Name="Finally" Width="400"  Height="500" TextWrapping="Wrap" ScrollViewer.VerticalScrollBarVisibility="Visible" Margin="0,20,0,0"/>
            </StackPanel>
            <Button HorizontalAlignment="Center" Height="50" Width="300" FontSize="26" Content="SAVE GAME LOG" Click="saveGameLog_Click" Margin="0,-50,0,0" BorderBrush="Black" BorderThickness="2"/>
            <TextBlock x:Name="saveStatus" HorizontalAlignment="Center" Margin="0,5,0,0"/>
        </StackPanel>
    </Grid>
</Page>
